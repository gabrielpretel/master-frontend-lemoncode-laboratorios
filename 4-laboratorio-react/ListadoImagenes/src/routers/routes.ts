export interface SwitchRoutes {
  champions: string;
  regions: string;
  checkout: string;
}

export const routes: SwitchRoutes = {
  champions: "/champions",
  regions: "/regions",
  checkout: "/checkout",
};
